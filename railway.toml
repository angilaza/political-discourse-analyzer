[build]
builder = "nixpacks"

[deploy]
startCommand = "poetry run uvicorn src.political_discourse_analyzer.core.main:app --host 0.0.0.0 --port $PORT"

[variables]
PYTHON_VERSION = "3.11"
POETRY_VERSION = "1.7.1"